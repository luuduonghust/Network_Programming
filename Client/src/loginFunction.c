// include các thư viện cần thiết cho code bên dưới
int loginFunction(int clientSocket)
{
    // Xử lý đăng nhập theo kịch bản

    // Trả về 1 nếu đăng nhập thành công, trả về 0 nếu đăng nhập thất bại => Chuyển hướng menu trong client.c
}