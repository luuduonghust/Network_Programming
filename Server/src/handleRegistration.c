#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <pthread.h>

void *handleRegistration(void *arg){
    char *message;
    recv(client_socket, mesa)
    recv(message, client_socket);
    int protocolCode;
    char* fullname;
    char* username;
    char* password;

    sscanf(message, "%d/n%s/n%s/n%s/r/n", &protocolCode, fullname, username, password);

}
